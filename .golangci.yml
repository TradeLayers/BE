version: "2"

linters:
  enable:
    # Correctness
    - errcheck
    - govet
    - staticcheck
    - typecheck

    # Bug prevention
    - bodyclose
    - nilerr
    - sqlclosecheck

    # Code quality
    - unused
    - ineffassign
    - gosimple
    - gocritic

    # Style
    - gofmt
    - goimports
    - revive

    # Security
    - gosec

  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - performance

    revive:
      rules:
        - name: exported
          disabled: true

  exclusions:
    paths:
      - vendor/
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck

run:
  timeout: 5m
